import {createClient} from "@/lib/supabase/server";import NewCampaignForm from "./new-campaign-form";import type {BrandVoice} from "@/lib/types";
export default async function NewCampaign(){const db=await createClient();const {data}=await db.from("brand_voices").select("*").order("created_at");return <main className="container"><div className="eyebrow">Begin with what you’re offering</div><h1>Create a campaign</h1><p className="muted">Share the heart of your offering. Healing Echoes will shape it into five pieces in your voice.</p><NewCampaignForm voices={(data||[]) as BrandVoice[]}/></main>}
