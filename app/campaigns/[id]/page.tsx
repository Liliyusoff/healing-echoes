import {createClient} from "@/lib/supabase/server";import {notFound} from "next/navigation";import CampaignEditor from "./campaign-editor";import type {Campaign,Piece} from "@/lib/types";
export default async function Detail({params}:{params:Promise<{id:string}>}){const {id}=await params;const db=await createClient();const [{data:campaign},{data:pieces}]=await Promise.all([db.from("campaigns").select("id,title,status,exported_at,offerings(id,name,offering_type,description,target_audience,scheduled_date,price_display,location_or_format,brand_voice_id)").eq("id",id).maybeSingle(),db.from("content_pieces").select("*").eq("campaign_id",id).order("created_at")]);if(!campaign)notFound();return <CampaignEditor campaign={campaign as unknown as Campaign} initialPieces={(pieces||[]) as Piece[]}/>}
